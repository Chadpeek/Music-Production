{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ProducerOS Configuration",
  "type": "object",
  "properties": {
    "default_mode": {
      "type": "string",
      "enum": ["analyze", "dry-run", "copy", "move"],
      "description": "Default run mode when the wizard is first opened"
    },
    "overwrite_nfo": {"type": "boolean"},
    "normalize_pack_name": {"type": "boolean"},
    "ignore_rules": {
      "type": "array",
      "items": {"type": "string"}
    }
  },
  "additionalProperties": true
}